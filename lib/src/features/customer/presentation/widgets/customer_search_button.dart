import 'package:flutter/material.dart';

class CustomerSearchButton extends StatelessWidget {
  const CustomerSearchButton({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: const Icon(Icons.search),
      onPressed: () {},
    );
  }
}